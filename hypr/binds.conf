# Definición de la tecla modificadora principal (CRÍTICO)
$mod = SUPER

# ==========================================
# LANZADOR Y UTILIDADES BÁSICAS
# ==========================================
bind = $mod, Return, exec, kitty
bind = $mod, Q, killactive
bind = $mod, M, exit
bind = $mod, E, exec, kitty -e yazi
bind = $mod, V, togglefloating
bind = $mod, R, exec, wofi --show drun
bind = $mod, F, fullscreen

# ==========================================
# NAVEGACIÓN Y FOCO
# ==========================================
# Ciclar ventanas (Estilo Alt-Tab)
bind = $mod, Tab, cyclenext
bind = $mod, Tab, bringactivetotop

# Mover el foco (Soporta Flechas y atajos Vim)
bind = $mod, left,  movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up,    movefocus, u
bind = $mod, down,  movefocus, d
bind = $mod, H,     movefocus, l
bind = $mod, L,     movefocus, r
bind = $mod, K,     movefocus, u
bind = $mod, J,     movefocus, d

# ==========================================
# WORKSPACES
# ==========================================
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9

# ==========================================
# GESTIÓN DE LA CUADRÍCULA Y VENTANAS
# ==========================================
# Mover ventana al workspace
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9

# Mover la ventana físicamente por la pantalla (Flechas y Vim)
bind = $mod SHIFT, left,  movewindow, l
bind = $mod SHIFT, right, movewindow, r
bind = $mod SHIFT, up,    movewindow, u
bind = $mod SHIFT, down,  movewindow, d
bind = $mod SHIFT, H,     movewindow, l
bind = $mod SHIFT, L,     movewindow, r
bind = $mod SHIFT, K,     movewindow, u
bind = $mod SHIFT, J,     movewindow, d

# Redimensionar ventanas fluidamente (Mantener SUPER + CTRL)
binde = $mod CTRL, left,  resizeactive, -30 0
binde = $mod CTRL, right, resizeactive, 30 0
binde = $mod CTRL, up,    resizeactive, 0 -30
binde = $mod CTRL, down,  resizeactive, 0 30

# ==========================================
# SCRATCHPADS (Pyprland)
# ==========================================
bind = $mod SHIFT, T, exec, pypr toggle term
bind = $mod SHIFT, N, exec, pypr toggle notes
bind = $mod SHIFT, M, exec, pypr toggle music
bind = $mod SHIFT, S, exec, pypr toggle serial

# ==========================================
# CAPTURAS DE PANTALLA Y MULTIMEDIA
# ==========================================
bind = , Print,       exec, grim ~/capturas/screenshot_$(date +%s).png
bind = SHIFT, Print,  exec, grim -g "$(slurp)" ~/capturas/region_$(date +%s).png

# ==========================================
# INTEGRACIÓN CON RATÓN (Pro Workflow)
# ==========================================
# Mantener SUPER + Click Izquierdo para arrastrar la ventana flotando/tiling
bindm = $mod, mouse:272, movewindow

# Mantener SUPER + Click Derecho para redimensionar la ventana de forma fluida
bindm = $mod, mouse:273, resizewindow
# Teclas Multimedia
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindl  = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
